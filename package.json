{
  "name": "dockerhub-keepalive",
  "version": "0.1.3",
  "description": "Docker Hub 镜像保活工具——自动定时 pull 指定镜像完成保活，完成后自动删除释放空间，三通道通知",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-force-exit tests/backend.test.js tests/qa.test.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "argon2": "^0.45.1",
    "better-sqlite3": "^13.0.2",
    "cookie-parser": "^1.4.7",
    "croner": "^10.0.1",
    "dockerode": "^5.0.1",
    "express": "^5.2.1",
    "nodemailer": "^9.0.3",
    "otplib": "^13.4.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}

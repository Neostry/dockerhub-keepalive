# CHANGELOG

> 版本变更记录（Keep a Changelog 风格，语义化版本）。

## [1.0.0] - 2026-08-03

首发版本：Docker Hub 镜像保活工具（dockerhub-keepalive）MVP 完整功能，测试通过（自动化 41/41 + 端到端），可部署发布。

### Added

- **账号与安全**
  - 首次设置引导（单用户，无默认密码，密码 ≥8 位含字母数字）
  - 登录鉴权：密码 Argon2id 哈希、统一错误文案（不区分账号/密码）、会话 HttpOnly Cookie（token 哈希存储，改密/改用户名吊销全部会话）
  - 可选 2FA（TOTP，RFC 6238）：密钥文本 + 二维码 + 复制按钮；关闭需动态码/密码确认；支持重新生成密钥
  - 登录失败限速：指数退避（1/2/4/8/16s）+ 达阈值临时锁定（默认 5 次 / 15 分钟，按 IP 持久化，重启不绕过）
  - 登录保护开关（F5a）：默认开启，可关闭登录页适配外部访问控制（关闭需确认 + 风险提示）
- **任务配置与调度**
  - 两种镜像输入：Docker Hub 用户名扫描（分页、估算容量、数量上限、失败标注）与单个镜像路径（多行批量、格式校验）
  - 任务 CRUD：新增/编辑/启用/停用/删除；任务详情（仓库元信息快照 + 头像，失败回退）
  - 独立 cron 调度（标准 5 段，非法即时提示；默认示例每月 1 日 03:00）；手动立即执行（P1）
- **保活执行**
  - cron 触发 → 空间预检（statfs，不足自动跳过 + 告警）→ 逐个 pull → 成功后立即 rmi 删除释放空间
  - 全局串行队列 + 任务级单飞锁 + 任务内受控并发（空间充足自动升 3）
  - pull/rmi 失败自动重试（默认 3 次、指数退避 5s/15s/45s）
  - 清理兜底：可选 `image prune` 深度清理（仅 dangling）+ 可选定时重启容器 + 「立即执行清理」
- **日志与通知**
  - 任务日志：执行时间/触发方式/逐镜像明细/耗时/清理结果，时间倒序分页
  - 三通道通知互备：站内信（通知中心，未读/已读/一键全读）+ Telegram + SMTP（465 TLS / 587 STARTTLS / 25 明文），失败自动重试
  - 通知凭证控制中心表单填写，AES-256-GCM 加密入库（主密钥 `APP_SECRET_KEY`，AAD 绑定通道，仅写不读），数据库无明文
- **系统与部署**
  - SQLite 单文件持久化（WAL，10 表），数据卷备份/迁移
  - Docker compose 一键部署（多阶段构建镜像，docker.sock 操作宿主 Docker，`/:/host:ro` 空间预检，healthcheck `/api/health`，`APP_SECRET_KEY` 缺失 fail-fast）
  - WebUI：8 页面（首次设置/登录含 2FA 两段式/任务/日志/通知/控制中心/错误页）+ 产品 logo SVG；简体中文界面
  - 安全响应头（CSP `default-src 'self'` 等）、`TRUST_PROXY`/`COOKIE_SECURE` 反代适配

### Changed

- （首发版本，无变更项）

### Fixed

- （首发版本，无修复项；开发期 P0/P2/P3 问题均已关闭，见 _vibe-coding/docs/07_测试与质量/问题清单_Bug_List.md）

### Security

- 凭证 AES-256-GCM 加密存储、仅写不读；会话 token 哈希；登录限速锁定；登录保护默认开启；敏感信息零硬编码、`.env` 不入库

## [Unreleased]

- 待定（以社区反馈为准；PRD「暂不做什么」边界内的多用户/私有仓库/统计面板等暂不承诺）

[1.0.0]: https://github.com/Neostry/dockerhub-keepalive/releases/tag/v1.0.0
